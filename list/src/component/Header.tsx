import React from "react";

const Header: React.FC = () => {
    return (
        <div style={{ padding: "12px", width: "100%", height: "100px", backgroundColor: "#fff", color: "#000" }}>
            이거슨 리스트에서 수정한 헤더
        </div>
    );
};

export default Header;
